# RemixEngine — Environment Variables
# Copy this file to .env.local and fill in the values.
# Never commit .env.local to version control.

# ============================================
# Supabase
# ============================================
# Your project URL from the Supabase dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anon (public) key — safe to expose to browser
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Service role key — NEVER expose to browser. Worker and server-side only.
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# ============================================
# App
# ============================================
# Public URL of the app (used for redirect URLs, og tags, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Webhook base URL — where external services send callbacks.
# Usually same as NEXT_PUBLIC_APP_URL unless using ngrok/tunnel in dev.
WEBHOOK_BASE_URL=http://localhost:3000

# ============================================
# Redis
# ============================================
# Redis connection URL for BullMQ job queues.
# Railway: set automatically. Local: redis://localhost:6379
REDIS_URL=redis://localhost:6379

# ============================================
# External APIs
# ============================================
# YouTube Data API v3 — for video metadata lookup
YOUTUBE_DATA_API_KEY=

# Google Gemini — for script remixing, title/thumbnail generation
GOOGLE_GEMINI_API_KEY=

# Google Gemini Image Generation (model: gemini-3-pro-image-preview, aka "Nano Banana")
# Separate API key for image generation — may use same key as GOOGLE_GEMINI_API_KEY
GOOGLE_GEMINI_IMAGE_API_KEY=

# fal.ai — for AI image generation (thumbnails, B-roll stills, fallback)
FAL_KEY=

# ElevenLabs — for voiceover generation (per scene)
ELEVENLABS_API_KEY=

# HeyGen — for avatar video generation
# Use Upload Asset API (audio_asset_id) — NEVER audio_url
HEYGEN_API_KEY=

# Runway ML — for B-roll video generation (image-to-video)
RUNWAY_API_KEY=

# Kling — alternative B-roll provider (optional)
KLING_API_KEY=
